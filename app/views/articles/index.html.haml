%h1 this is the articles#index placeholder

- @articles.each do |a|
  %h2= a.title
  %p
    Published at
    = a.created_at.strftime('%d %b, %Y')
  %p= truncate(a.content, length: 300)

= link_to "Nouvel article", new_article_path
